#include <stdio.h>
/**
 * main - Entry point
 *
 * Return 0: Always
 * uses printf to display a string
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}
