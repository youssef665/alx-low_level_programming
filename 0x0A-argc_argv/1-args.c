#include <stdio.h>

/**
 * main - Entry point
 * @argc: an integer argument counter
 * @argv: a pointer to an array of strings
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{

	printf("%d\n", argc);

	return (0);
}
