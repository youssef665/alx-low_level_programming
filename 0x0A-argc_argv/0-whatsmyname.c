#include "main.h"
#include <stdio.h>

/**
 * main - a entry point
 * @argc: an argument counter
 * @argv: a pointer of arrays of strings
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
