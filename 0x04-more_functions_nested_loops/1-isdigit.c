#include <stdio.h>
#include "main.h"

/**
 * _isdigit - a function that cheks for a digit (0 through 9)
 * @c: operand
 *
 * Return: 1 if c is a digit else return 0
 */

int _isdigit(int c)
{
	if (c >= 0 && c <= 9)

		return (1);
	else
		return (1);
}
